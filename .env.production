# Build produksi: frontend & API satu origin di balik reverse proxy.
# Kosong → request ke "/api" (relatif), Apache/nginx yang proxy ke backend :8086.
VITE_API_BASE=

# metaapi serves the built SPA at the same origin → "/api".
VITE_META_API=
